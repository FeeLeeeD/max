{
  "name": "@max/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "hello": "tsx src/hello.ts",
    "build": "tsc --build",
    "dev": "tsx watch src/hello.ts",
    "clean": "rm -rf dist *.tsbuildinfo",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@max/shared": "workspace:*"
  }
}
